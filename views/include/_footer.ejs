<footer class="footer">
    <p>&copy; 2023 User Management System</p>
</footer>
<script>
    $("#form").submit(function (event) {
        event.preventDefault();


        var unindex_array = $(this).serializeArray();
        console.log(unindex_array);

        var data = {}

        $.map(unindex_array, function (n, i) {
            data[n['name']] = n['value'];
        })

        console.log(data);

        var request = {
            'url': `http://localhost:3595/api/create/${data.id}`,
            'method': 'put',
            'data': data
        }

        $.ajax(request).done(function (response) {
            alert("Successfully updated")
        })

    })

    $('.del').click(function () {
        console.log("clicked")
        var id = $(this).attr('data-id');
        var request = {
            'url': `http://localhost:3595/api/create/${id}`,
            'method': 'delete',
        }

        if (confirm("Do you want to delete this record")) {
            $.ajax(request).done(function (response) {
                alert("Successfully deleted")
                location.reload();
            })

        }
    })
</script>
</body>

</html>